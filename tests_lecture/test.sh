#! /bin/bash
# that's called a 'shebang' it means run this text file through that program
# when you execute the file as a program

python3 -m unittest discover tests

# to make this file executable, chmod u+x test.sh
# then you can run it as a program with
# ./test.sh